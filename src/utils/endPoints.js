

const VERSION = "/api/v1";
const OWNER = `${VERSION}/owners`
const USER = `${VERSION}/users`
const ADMIN = `${VERSION}/admins`
const CATEGORY = `${VERSION}/categories`




module.exports = {
    ADMIN,
    OWNER,
    USER,
    CATEGORY
}



